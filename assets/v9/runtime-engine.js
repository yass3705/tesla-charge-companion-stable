(function(root,factory){
  if(typeof module==='object'&&module.exports){
    module.exports=factory(require('./data-engine.js'),require('./offer-engine.js'),require('./session-engine.js'),require('./station-score-engine.js'),require('./routing-engine.js'),require('./vehicle-profile-engine.js'),require('./session-planner-engine.js'),require('./cross-border-subscriptions.js'));
  }else{
    root.TCCV9RuntimeEngine=factory(root.TCCV9DataEngine,root.TCCV9OfferEngine,root.TCCV9SessionEngine,root.TCCV9StationScoreEngine,root.TCCV9RoutingEngine,root.TCCV9VehicleProfileEngine,root.TCCV9SessionPlannerEngine,root.TCCV9CrossBorderSubscriptions);
  }
})(typeof globalThis!=='undefined'?globalThis:this,function(DataEngine,OfferEngine,SessionEngine,StationScoreEngine,RoutingEngine,VehicleProfileEngine,SessionPlannerEngine,CrossBorder){
  'use strict';
  if(!DataEngine||!OfferEngine||!SessionEngine||!StationScoreEngine||!RoutingEngine||!VehicleProfileEngine||!SessionPlannerEngine)throw new Error('TCC V9 runtime dependency missing');
  const text=v=>String(v==null?'':v).trim();
  const country=v=>text(v).toUpperCase();
  const uniq=values=>[...new Set((values||[]).filter(Boolean))];
  const number=v=>{const n=Number(v);return Number.isFinite(n)?n:null;};

  function coverageIndex(registry){const map=new Map();for(const raw of registry?.subscriptionCoverage||[]){const id=text(raw.subscriptionId||raw.id);if(!id)continue;map.set(id,{countries:uniq((raw.countries||[]).map(country)),globalCoverage:raw.globalCoverage===true||(raw.countries||[]).includes('*'),operatorIds:uniq((raw.operatorIds||[]).map(OfferEngine.providerId)),evidenceSources:uniq(raw.evidenceSources||[])});}return map;}
  function applyCoverage(rows,index){return(rows||[]).map(row=>{const coverage=index.get(text(row.id));if(!coverage)return row;const globalCoverage=row.globalCoverage===true||coverage.globalCoverage===true;const countries=uniq([...(row.countries||[]),...coverage.countries.filter(c=>c!=='*')]).sort();return{...row,countries,globalCoverage,countryCount:globalCoverage?null:countries.length,operatorIds:uniq([...(row.operatorIds||[]),...coverage.operatorIds]).sort(),coverageEvidenceSources:coverage.evidenceSources};});}
  function filterSubscriptions(rows,filters={}){const min=number(filters.minCountries),max=number(filters.maxCountries),wanted=(filters.countryCodes||[]).map(country).filter(Boolean),mode=filters.coverageMode==='all'?'all':'any',providers=(filters.providerIds||[]).map(OfferEngine.providerId),operators=(filters.operatorIds||[]).map(OfferEngine.providerId);return(rows||[]).filter(row=>{const score=row.globalCoverage?Infinity:(row.countries||[]).length;if(min!=null&&score<min)return false;if(max!=null&&score>max)return false;if(providers.length&&!providers.includes(row.providerId))return false;if(operators.length&&!operators.some(id=>(row.operatorIds||[]).includes(id)))return false;if(wanted.length&&!row.globalCoverage){const matches=wanted.map(c=>(row.countries||[]).includes(c));if(mode==='all'&&!matches.every(Boolean))return false;if(mode==='any'&&!matches.some(Boolean))return false;}return true;}).sort((a,b)=>{if(a.globalCoverage!==b.globalCoverage)return a.globalCoverage?-1:1;const ca=a.countryCount??Infinity,cb=b.countryCount??Infinity;if(ca!==cb)return cb-ca;return`${a.providerId}|${a.label}|${a.id}`.localeCompare(`${b.providerId}|${b.label}|${b.id}`);});}
  function mergeSubscriptionRows(rows){const map=new Map();for(const row of rows||[]){const key=text(row.id);if(!key)continue;const current=map.get(key);if(!current){map.set(key,{...row,countries:[...(row.countries||[])],operatorIds:[...(row.operatorIds||[])],sourceIds:[...(row.sourceIds||[])]});continue;}const globalCoverage=current.globalCoverage===true||row.globalCoverage===true,countries=uniq([...(current.countries||[]),...(row.countries||[])]).sort();map.set(key,{...current,...row,countries,globalCoverage,countryCount:globalCoverage?null:countries.length,operatorIds:uniq([...(current.operatorIds||[]),...(row.operatorIds||[])]).sort(),sourceIds:uniq([...(current.sourceIds||[]),...(row.sourceIds||[])]).sort()});}return[...map.values()];}
  function exactStationOverride(query,station,subscriptionId){const all=query?.subscriptionStationPrices||{},byStation=all[station?.id]||all[station?.canonicalId]||{};return byStation?.[subscriptionId]||null;}
  function applySelectedSubscriptions(station,selectedSubscriptions=[],crossBorderConfig={},query={}){
    if(!CrossBorder||!crossBorderConfig?.policy||!(selectedSubscriptions||[]).length)return station;
    let out={...station,offers:[...(station?.offers||[])]},advisories=[];const policyById=new Map((crossBorderConfig.policy.subscriptions||[]).map(s=>[text(s.id),s]));
    for(const id of selectedSubscriptions||[]){const subscription=policyById.get(text(id));if(!subscription)continue;const override=exactStationOverride(query,station,id),resolved=CrossBorder.resolve({subscriptionId:id,countryCode:station.countryCode||query.countryCode,physicalOperator:station.physicalOperator,exactStationPrice:override?.pricePerKwh,exactStationCurrency:override?.currency},crossBorderConfig),offer=CrossBorder.toOffer(resolved,subscription,station);if(!offer)continue;if(resolved.rankable===true)out=OfferEngine.mergeStationOffers(out,[offer],{countryCode:station.countryCode||query.countryCode});else advisories.push(offer);}
    out.eligibleOffers=OfferEngine.eligibleOffers(out,selectedSubscriptions,{countryCode:out.countryCode||query.countryCode});out.rankableOffers=[...(out.eligibleOffers||[])];out.subscriptionAdvisories=OfferEngine.dedupeOffers(advisories,{countryCode:out.countryCode||query.countryCode});return out;
  }
  function boundedStations(allStations,baseRoutingCandidates,limit){const n=Math.floor(number(limit)||0);if(n<=0||allStations.length<=n)return allStations;const byId=new Map(allStations.map(st=>[st.id,st])),chosen=new Map();for(const candidate of baseRoutingCandidates||[]){const st=byId.get(candidate.id);if(st&&!chosen.has(st.id)){chosen.set(st.id,st);if(chosen.size>=n)break;}}for(const st of allStations){if(chosen.size>=n)break;if(!chosen.has(st.id))chosen.set(st.id,st);}return[...chosen.values()];}

  function createEngine(config={}){
    const base=DataEngine.createEngine(config),coverage=coverageIndex(config.registry||{}),routeProvider=config.routeProvider||null,vehicleCatalog=VehicleProfileEngine.createCatalog(config.vehicleProfiles||[]),crossBorderConfig=config.crossBorderPricing||{};
    function deriveSubscriptionOptions(stations,filters={}){const local=OfferEngine.deriveSubscriptionOptions(stations,{}),cross=CrossBorder&&crossBorderConfig?.policy?CrossBorder.subscriptionOptions(crossBorderConfig):[];return filterSubscriptions(applyCoverage(mergeSubscriptionRows([...local,...cross]),coverage),filters);}
    async function queryArea(query={}){
      const area=await base.queryArea(query),preselected=uniq((query.selectedSubscriptions||query.session?.selectedSubscriptions||[]).map(text)),allStations=(area.stations||[]).map(st=>applySelectedSubscriptions(OfferEngine.mergeStationOffers(st,[],{countryCode:st.countryCode||query.countryCode}),preselected,crossBorderConfig,query)),allById=new Map(allStations.map(st=>[st.id,st])),baseRoutingCandidates=(area.routingCandidates||[]).map(st=>allById.get(st.id)||st),stations=boundedStations(allStations,baseRoutingCandidates,query.stationLimit),stationById=new Map(stations.map(st=>[st.id,st]));
      const routingCandidates=baseRoutingCandidates.map(st=>stationById.get(st.id)).filter(Boolean),subscriptions=deriveSubscriptionOptions(stations,query.subscriptionFilters||{});
      const resolvedVehicle=VehicleProfileEngine.resolve({catalog:vehicleCatalog,profileId:query.vehicleProfileId||query.session?.vehicleProfileId,profile:query.vehicleProfile,session:query.session||{}}),session=resolvedVehicle.session;
      let routeResult={byStationId:{},errors:[],routedCount:0,requestedCount:0,timedOut:false,aborted:false};const provider=query.routeProvider||routeProvider;
      if(query.session&&provider&&query.origin)routeResult=await RoutingEngine.routeCandidates(routingCandidates,{origin:query.origin,provider,session,concurrency:query.routingConcurrency||6,requestTimeoutMs:query.routingRequestTimeoutMs||6000,budgetMs:query.routingBudgetMs||25000});
      const routeMap={...(area.routes?.byStationId||area.routes||{}),...(query.route?.byStationId||query.route||{}),...(routeResult.byStationId||{})};
      let sessionPlans=null,sessionEvaluations=null,stationScores=null,rankedStations=null;
      if(query.session){
        sessionPlans=SessionPlannerEngine.planArea(stations,session,{route:{byStationId:routeMap}});
        const selectedSubscriptions=preselected.length?preselected:(session.selectedSubscriptions||[]),targetCurrency=query.targetCurrency||session.targetCurrency||'EUR',fxRates=query.fxRates||session.fxRates||{};
        const evalRows=stations.map(st=>({station:st,evaluation:SessionEngine.evaluateStation(st,sessionPlans[st.id]?.effectiveSession||session,{selectedSubscriptions,targetCurrency,fxRates})}));
        sessionEvaluations=Object.fromEntries(evalRows.map(row=>[row.evaluation.stationId,row.evaluation]));
        const scoreRows=stations.map(st=>({station:st,score:StationScoreEngine.scoreStation(st,sessionEvaluations[st.id],sessionPlans[st.id]?.effectiveSession||session,{route:{byStationId:routeMap},plan:sessionPlans[st.id]})}));
        const scored=StationScoreEngine.sortRows(scoreRows,query.sortBy||session.sortBy||'finalCost');
        stationScores=Object.fromEntries(scored.map(row=>[row.score.stationId,row.score]));rankedStations=scored.map(row=>row.station);
      }
      const missedTargets=sessionPlans?Object.values(sessionPlans).filter(p=>p&&!p.targetReached).length:0,stationLimit=number(query.stationLimit);
      return{...area,stations,routingCandidates,subscriptions,selectedSubscriptions:preselected,vehicleProfile:resolvedVehicle.profile,effectiveSession:session,routes:{byStationId:routeMap},routeResult,sessionPlans,sessionEvaluations,stationScores,rankedStations,diagnostics:{...(area.diagnostics||{}),sourceStationCount:allStations.length,stationLimitApplied:stationLimit!=null&&stationLimit>0&&allStations.length>stations.length,stationLimit:stationLimit!=null&&stationLimit>0?Math.floor(stationLimit):null,subscriptionOptionCount:subscriptions.length,selectedSubscriptionCount:preselected.length,crossBorderPricingEnabled:!!(CrossBorder&&crossBorderConfig?.policy),vehicleProfileId:resolvedVehicle.profile?.id||null,vehicleProfileResolved:!!resolvedVehicle.profile,routingRequestedCount:routeResult.requestedCount,routedStationCount:routeResult.routedCount,routingErrorCount:routeResult.errors.length,routingTimedOut:routeResult.timedOut===true,routingAborted:routeResult.aborted===true,routingDurationMs:number(routeResult.durationMs),sessionPlannedStationCount:sessionPlans?Object.keys(sessionPlans).length:0,targetMissedStationCount:missedTargets,sessionEvaluatedStationCount:sessionEvaluations?Object.keys(sessionEvaluations).length:0,sessionComparableStationCount:sessionEvaluations?Object.values(sessionEvaluations).filter(x=>x?.best).length:0,scoredStationCount:stationScores?Object.keys(stationScores).length:0,fullyScoredStationCount:stationScores?Object.values(stationScores).filter(x=>x?.complete?.pricing&&x?.complete?.route&&x?.complete?.charging&&x?.complete?.session).length:0}};
    }
    return{...base,queryArea,vehicleProfiles:vehicleCatalog.list,vehicleProfile:id=>vehicleCatalog.get(id),eligibleOffers:OfferEngine.eligibleOffers,deriveSubscriptionOptions,applySelectedSubscriptions:(station,selected,query={})=>applySelectedSubscriptions(station,selected,crossBorderConfig,query),dedupeOffers:OfferEngine.dedupeOffers,mergeStationOffers:OfferEngine.mergeStationOffers,evaluateStation:SessionEngine.evaluateStation,evaluateArea:SessionEngine.evaluateArea,scoreStation:StationScoreEngine.scoreStation,scoreArea:StationScoreEngine.scoreArea,routeCandidates:RoutingEngine.routeCandidates,planStation:SessionPlannerEngine.planStation,planArea:SessionPlannerEngine.planArea};
  }
  return{createEngine,applyCoverage,filterSubscriptions,mergeSubscriptionRows,applySelectedSubscriptions,boundedStations};
});
