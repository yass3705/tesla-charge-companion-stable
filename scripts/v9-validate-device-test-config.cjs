#!/usr/bin/env node
'use strict';

const fs=require('node:fs');
const path=require('node:path');
const FILES={rollout:'data/v9/rollout-config.json',selfEnrollment:'data/v9/self-enrollment-config.json',readiness:'data/v9/access-readiness.json',devicePolicy:'data/v9/device-test-policy.json',canaryPolicy:'data/v9/canary-policy.json'};
function readConfiguration(root=process.cwd()){return Object.fromEntries(Object.entries(FILES).map(([key,relativePath])=>[key,JSON.parse(fs.readFileSync(path.join(root,relativePath),'utf8'))]));}
function failed(checks){return checks.filter(([,valid])=>!valid).map(([message])=>message);}
function validateConfiguration(configuration,{now=new Date(),maximumWindowMinutes=60}={}){
  const rollout=configuration?.rollout||{},self=configuration?.selfEnrollment||{},readiness=configuration?.readiness||{},policy=configuration?.devicePolicy||{},canaryPolicy=configuration?.canaryPolicy||{};
  const checkedAt=new Date(now),nowMs=checkedAt.getTime(),maximumWindowMs=Number(maximumWindowMinutes)*60*1000;
  const commonErrors=failed([['validation time must be valid',Number.isFinite(nowMs)],['rollout stage must be preview or canary',['preview','canary'].includes(rollout.stage)],['kill switch must remain off for a deployable configuration',rollout.killSwitch!==true],['rollout must require readiness',rollout.requireReadiness!==false],['candidate routes must target v9-app/',rollout.canaryPath==='v9-app/'&&rollout.productionPath==='v9-app/']]);
  const closedErrors=failed([['closed rollout stage must be preview',rollout.stage==='preview'],['closed random canary must be 0%',Number(rollout.canaryPercent)===0],['access readiness must be BLOCKED',readiness.ready===false&&readiness.verdict==='BLOCKED'],['self-enrollment must be disabled',self.enabled===false],['self-enrollment readiness approval must be false',self.readinessApproved===false],['closed state must not retain a token hash',!String(self.tokenSha256||'').trim()],['closed state must not retain an expiry',self.expiresAt==null],['device-test policy must be disabled',policy.enabled===false]]);
  if(commonErrors.length===0&&closedErrors.length===0)return{ok:true,mode:'CLOSED',checkedAt:checkedAt.toISOString(),errors:[]};
  const expiresAtMs=Date.parse(self.expiresAt),openedAtMs=Date.parse(readiness.updatedAt),configuredWindowMinutes=Number(policy.maxWindowMinutes),maximumGrantMinutes=Number(self.maxGrantMinutes);
  const controlledErrors=failed([['controlled rollout stage must remain preview',rollout.stage==='preview'],['controlled random canary must remain 0%',Number(rollout.canaryPercent)===0],['access readiness must be READY',readiness.ready===true&&readiness.verdict==='READY'],['self-enrollment must be enabled',self.enabled===true],['self-enrollment must require readiness',self.requireReadiness!==false],['self-enrollment readiness must be approved',self.readinessApproved===true],['active token must be represented by one SHA-256 hash',/^[a-f0-9]{64}$/i.test(String(self.tokenSha256||'').trim())],['active token version must be set',Boolean(String(self.tokenVersion||'').trim())],['active window expiry must be valid',Number.isFinite(expiresAtMs)],['active window must not be expired',Number.isFinite(expiresAtMs)&&expiresAtMs>nowMs],['readiness update time must be valid',Number.isFinite(openedAtMs)],['readiness update time must not be in the future',Number.isFinite(openedAtMs)&&openedAtMs<=nowMs+5*60*1000],['active expiry must follow readiness approval',Number.isFinite(expiresAtMs)&&Number.isFinite(openedAtMs)&&expiresAtMs>openedAtMs],['active window must be bounded to 60 minutes',Number.isFinite(expiresAtMs)&&Number.isFinite(openedAtMs)&&expiresAtMs-openedAtMs<=maximumWindowMs],['grant duration must be between 1 and 60 minutes',Number.isFinite(maximumGrantMinutes)&&maximumGrantMinutes>=1&&maximumGrantMinutes<=maximumWindowMinutes],['device-test policy must be enabled',policy.enabled===true],['device-test policy must require readiness',policy.requireReadiness===true],['device-test policy must require canary 0%',policy.requireCanaryPercentZero===true],['device-test policy must auto-close on rollback',policy.autoCloseOnRollback===true],['device-test policy window must be between 1 and 60 minutes',Number.isFinite(configuredWindowMinutes)&&configuredWindowMinutes>=1&&configuredWindowMinutes<=maximumWindowMinutes],['grant duration must not exceed the policy window',Number.isFinite(maximumGrantMinutes)&&Number.isFinite(configuredWindowMinutes)&&maximumGrantMinutes<=configuredWindowMinutes],['configured policy must cover the active window',Number.isFinite(expiresAtMs)&&Number.isFinite(openedAtMs)&&Number.isFinite(configuredWindowMinutes)&&expiresAtMs-openedAtMs<=configuredWindowMinutes*60*1000],['device test must require at least 10 runs',Number(policy.minimumRuns)>=10],['device test must require at least 10 successful runs',Number(policy.minimumSuccessfulRuns)>=10]]);
  if(commonErrors.length===0&&controlledErrors.length===0)return{ok:true,mode:'CONTROLLED_WINDOW',checkedAt:checkedAt.toISOString(),expiresAt:new Date(expiresAtMs).toISOString(),tokenVersion:String(self.tokenVersion).trim(),errors:[]};
  const initialPercent=Number(canaryPolicy.initialPercent||1);
  const canaryErrors=failed([['canary rollout stage must be canary',rollout.stage==='canary'],['canary percent must equal the initial policy stage',Number(rollout.canaryPercent)===initialPercent&&initialPercent>0&&initialPercent<=100],['canary policy must be active',canaryPolicy.active===true],['canary policy must contain the active stage',(canaryPolicy.stages||[]).some(stage=>Number(stage.percent)===initialPercent)],['access readiness must be READY',readiness.ready===true&&readiness.verdict==='READY'],['V8 control must use the isolated v8-app path',rollout.v8Path==='v8-app/'],['self-enrollment must stay disabled during random canary',self.enabled===false],['self-enrollment readiness approval must stay false',self.readinessApproved===false],['random canary must not retain a token hash',!String(self.tokenSha256||'').trim()],['random canary must not retain a token expiry',self.expiresAt==null],['device-test policy must stay disabled during random canary',policy.enabled===false]]);
  if(commonErrors.length===0&&canaryErrors.length===0)return{ok:true,mode:'CANARY',checkedAt:checkedAt.toISOString(),canaryPercent:initialPercent,errors:[]};
  const candidates=[{mode:'CLOSED',errors:[...commonErrors,...closedErrors]},{mode:'CONTROLLED_WINDOW',errors:[...commonErrors,...controlledErrors]},{mode:'CANARY',errors:[...commonErrors,...canaryErrors]}].sort((a,b)=>a.errors.length-b.errors.length);
  return{ok:false,mode:'INVALID',checkedAt:Number.isFinite(nowMs)?checkedAt.toISOString():null,errors:candidates[0].errors};
}
function run(){const result=validateConfiguration(readConfiguration());if(!result.ok){console.error(`BLOCKED: invalid V9 access configuration\n- ${result.errors.join('\n- ')}`);process.exitCode=1;return;}console.log(`PASS V9 access configuration: ${result.mode}`);if(result.expiresAt)console.log(`expiresAt=${result.expiresAt}`);if(result.canaryPercent)console.log(`canaryPercent=${result.canaryPercent}`);}
if(require.main===module)run();
module.exports={FILES,readConfiguration,validateConfiguration};
