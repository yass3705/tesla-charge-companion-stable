name: TCC Actions probe

on:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/tcc-actions-probe.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Vérifier le démarrage Actions
        run: echo "TCC Actions probe OK"
