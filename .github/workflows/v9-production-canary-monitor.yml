name: V9 production watchdog

on:
  schedule:
    - cron: '17 */2 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'v9-production-control.json'
      - 'v9-production-shell/shell-config.json'
      - 'v9-production-runtime/**'
      - 'scripts/v9-production-canary-monitor.cjs'
      - '.github/workflows/v9-production-canary-monitor.yml'

permissions:
  actions: read
  contents: read

concurrency:
  group: v9-production-watchdog
  cancel-in-progress: false

jobs:
  observe:
    name: Check live production
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout current production state
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate live routing, readiness, pins and fail-closed controls
        env:
          BASE_URL: https://yass3705.github.io/tesla-charge-companion-stable
          SAMPLE_SIZE: '10000'
          RESULT_PATH: v9-production-watchdog-result.json
        run: node scripts/v9-production-canary-monitor.cjs

      - name: Annotate observation provenance
        if: always()
        shell: bash
        run: |
          if [ -f v9-production-watchdog-result.json ]; then
            node - <<'NODE'
          const fs = require('fs');
          const file = 'v9-production-watchdog-result.json';
          const result = JSON.parse(fs.readFileSync(file, 'utf8'));
          let eventPayload = {};
          try {
            eventPayload = JSON.parse(fs.readFileSync(process.env.GITHUB_EVENT_PATH, 'utf8'));
          } catch (_) {}
          const headCommitMessage = eventPayload?.head_commit?.message || null;
          result.github = {
            eventName: process.env.GITHUB_EVENT_NAME || null,
            runId: process.env.GITHUB_RUN_ID || null,
            runNumber: process.env.GITHUB_RUN_NUMBER || null,
            runAttempt: process.env.GITHUB_RUN_ATTEMPT || null,
            workflow: process.env.GITHUB_WORKFLOW || null,
            ref: process.env.GITHUB_REF || null,
            sha: process.env.GITHUB_SHA || null,
            headCommitMessage,
          };
          result.scheduledWatchdogObservation = result.github.eventName === 'schedule';
          fs.writeFileSync(file, `${JSON.stringify(result, null, 2)}\n`);
          NODE
          fi

      - name: Publish watchdog summary
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f v9-production-watchdog-result.json ]; then
            cat v9-production-watchdog-result.json
            {
              echo '## V9 production watchdog'
              echo
              echo '```json'
              cat v9-production-watchdog-result.json
              echo '```'
            } >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'No watchdog result file was produced.' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Archive watchdog evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v9-production-watchdog-${{ github.run_id }}
          path: v9-production-watchdog-result.json
          if-no-files-found: warn
          retention-days: 14
