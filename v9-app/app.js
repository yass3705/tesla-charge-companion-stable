(async function(){'use strict';const $=id=>document.getElementById(id),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const status=$('status'),stationsEl=$('stations'),summaryEl=$('summary'),candidateState=$('candidateState'),telemetryEl=$('telemetry'),identityEl=$('deviceIdentity'),bundleEl=$('enrollmentBundle'),enrollmentStatus=$('enrollmentStatus');let engine=null;const identity=window.TCCV9CanaryIdentity.get(),bundle=window.TCCV9EnrollmentEngine.enrollmentBundle(identity,{label:'manual-test-device',notes:'Add explicitly to data/v9/whitelist-config.json only after readiness review.'});candidateState.textContent='Candidat V9 isolé · trafic réel 0 %';identityEl.textContent=identity;bundleEl.value=window.TCCV9EnrollmentEngine.stringifyBundle(bundle);async function copyText(value,label){try{await navigator.clipboard.writeText(value);enrollmentStatus.textContent=`${label} copié. Aucune modification distante n’a été effectuée.`;enrollmentStatus.className='ok';}catch(_){enrollmentStatus.textContent='Copie automatique indisponible : sélectionne le texte manuellement.';enrollmentStatus.className='warn';}}$('copyIdentity').addEventListener('click',()=>copyText(identity,'Identifiant'));$('copyEnrollment').addEventListener('click',()=>copyText(bundleEl.value,'Bloc d’enrôlement'));function setStatus(m,k='muted'){status.className=k;status.textContent=m;}function fmt(v,d=1){return v==null?'—':Number(v).toFixed(d);}function renderTelemetry(){const m=window.TCCV9CanaryTelemetry.summary(),a=window.TCCV9CanaryTelemetry.rollbackAssessment(m);telemetryEl.textContent=`Runs ${m.runs} · échecs ${m.queryFailures} · erreurs routage ${m.routingErrors} · erreurs source ${m.sourceErrors} · latence moy. ${m.avgDurationMs??'—'} ms · décision ${a.decision}`;telemetryEl.className=a.rollback?'err':a.decision==='INSUFFICIENT_DATA'?'warn':'ok';}function maxPower(st){let m=0;for(const e of st.evses||[])for(const c of e.connectors||[])m=Math.max(m,Number(c.powerKw||0));return m;}function render(area){const stations=area.rankedStations||area.stations||[];summaryEl.textContent=`${(area.stations||[]).length} stations · ${area.diagnostics?.routedStationCount||0}/${area.diagnostics?.routingRequestedCount||0} routées · ${area.diagnostics?.fullyScoredStationCount||0} scorées`;stationsEl.innerHTML=stations.slice(0,50).map(st=>{const ev=area.sessionEvaluations?.[st.id],score=area.stationScores?.[st.id],plan=area.sessionPlans?.[st.id];return `<div class="station"><strong>${esc(st.name)}</strong><div class="muted">${esc(st.physicalOperator?.name)} · ${maxPower(st)} kW</div>${ev?.best?`<div class="best">${Number(ev.best.total).toFixed(2)} ${esc(ev.best.targetCurrency)} · ${esc(ev.best.provider)}</div>`:'<div class="best muted">Aucune offre comparable</div>'}${plan?`<div class="offer">SOC arrivée ${fmt(plan.arrivalSoc)}% → ${fmt(plan.actualTargetSoc)}%</div>`:''}${score?`<div class="offer">Charge ${fmt(score.chargingMinutes)} min · total ${fmt(score.totalTimeMinutes)} min</div>`:''}</div>`;}).join('')||'<span class="muted">Aucune station.</span>';}
try{const [registry,vehicleProfiles]=await Promise.all([fetch('../data/v9/source-registry.json',{cache:'no-cache'}).then(r=>{if(!r.ok)throw new Error(`registry ${r.status}`);return r.json();}),fetch('../data/v9/vehicle-profiles.json',{cache:'no-cache'}).then(r=>{if(!r.ok)throw new Error(`profiles ${r.status}`);return r.json();})]);const adapters={teslaJson:window.TCCV9Adapters.teslaJson,nationalCompact:window.TCCV9Adapters.nationalCompact,directOffers:window.TCCV9Adapters.directOffers,legacyDirectTariffs:window.TCCV9Adapters.legacyDirectTariffs,legacyDirectStations:window.TCCV9Adapters.legacyDirectStations,franceEmspCompact:window.TCCV9Adapters.franceEmspCompact,franceCrosswalk:window.TCCV9Adapters.franceCrosswalk,franceIrveStatus:window.TCCV9Adapters.franceIrveStatus};const loaders=window.TCCV9BrowserLoaders.createRegistryLoaders({registry,basePath:'..',adapters}),routeProvider=window.TCCV9BrowserRouting.osrmProvider();engine=window.TCCV9RuntimeEngine.createEngine({registry,loaders,routeProvider,vehicleProfiles});window.TCCV9CandidateEngine=engine;$('vehicleProfile').innerHTML=engine.vehicleProfiles().map(p=>`<option value="${esc(p.id)}">${esc(p.label)}</option>`).join('');setStatus('Candidat V9 prêt.','ok');window.dispatchEvent(new CustomEvent('tcc-v9-candidate-ready'));}catch(err){window.TCCV9CanaryTelemetry.record('init',{ok:false,message:err.message});setStatus(`Initialisation impossible : ${err.message}`,'err');renderTelemetry();return;}renderTelemetry();$('country').addEventListener('change',()=>{const c=$('country').value;if(c==='NL'){$('lat').value='51.4416';$('lon').value='5.4697';}else if(c==='IT'){$('lat').value='41.9028';$('lon').value='12.4964';}else{$('lat').value='48.798';$('lon').value='2.061';}});$('load').addEventListener('click',async()=>{const started=performance.now();try{setStatus('Calcul candidat V9…');const selectedSubscriptions=$('selectedSubscriptions').value.split(',').map(x=>x.trim()).filter(Boolean),area=await engine.queryArea({countryCode:$('country').value,origin:{lat:Number($('lat').value),lon:Number($('lon').value)},radiusKm:Number($('radius').value||15),filters:{},vehicleProfileId:$('vehicleProfile').value,selectedSubscriptions,session:{startSoc:Number($('startSoc').value),targetSoc:Number($('targetSoc').value),targetCurrency:'EUR',startAt:$('startAt').value||null,disconnectAt:$('disconnectAt').value||null},sortBy:$('sortBy').value});render(area);const re=area.diagnostics?.routingErrorCount||0,se=area.diagnostics?.errors?.length||0;window.TCCV9CanaryTelemetry.record('query',{ok:se===0&&re===0,durationMs:performance.now()-started,stationCount:(area.stations||[]).length,routingErrorCount:re,sourceErrorCount:se,message:se||re?'runtime errors':''});setStatus(se||re?`Terminé avec ${se+re} erreur(s).`:'Terminé sans erreur.',se||re?'err':'ok');}catch(err){window.TCCV9CanaryTelemetry.record('query',{ok:false,durationMs:performance.now()-started,message:err.message});setStatus(`Erreur : ${err.message}`,'err');}renderTelemetry();});})();